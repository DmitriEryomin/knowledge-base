Type of NodeJS streams .
  * Readable Streams (for reading).
  * Writable Streams (for writing).
  * Transform Streams (both reading and writing).
  * Duplex Streams (transform data input and outputs transformed data).